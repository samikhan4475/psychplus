'use client'

import { ConsentsHistoryDialog } from '../consents-history-dialog'
import { RowActionProps } from '../types'

const RowActionHistory = (props: RowActionProps) => {
  return <ConsentsHistoryDialog {...props} />
}
export { RowActionHistory }
