/** @type {import("next").NextConfig} */
const config = {
  basePath: '/revcycle',
  reactStrictMode: true,
  swcMinify: true,
  output: 'standalone',
  transpilePackages: [],
}

module.exports = config
