import { type Metadata } from 'next'
import { RootLayout } from '@psychplus/components'

export const metadata: Metadata = {
  title: 'title',
  description: 'description',
}

export default RootLayout
