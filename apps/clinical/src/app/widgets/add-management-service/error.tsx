'use client'

import { ADD_MANAGEMENT_SERVICE_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const AddManagementServiceError = () => {
  return <WidgetError name={ADD_MANAGEMENT_SERVICE_WIDGET} />
}

export default AddManagementServiceError
