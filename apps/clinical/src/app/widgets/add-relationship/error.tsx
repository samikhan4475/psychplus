'use client'

import { ADD_RELATIONSHIP_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const AddAuthWidgetError = () => {
  return <WidgetError name={ADD_RELATIONSHIP_WIDGET} />
}

export default AddAuthWidgetError
