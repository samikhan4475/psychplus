'use client'

import { ENLARGE_IMAGE_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const EnlargeImageWidgetError = () => {
  return <WidgetError name={ENLARGE_IMAGE_WIDGET} />
}

export default EnlargeImageWidgetError
