'use client'

import { CREATE_REFERRAL_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const CreateReferralWidgetError = () => {
  return <WidgetError name={CREATE_REFERRAL_WIDGET} />
}

export default CreateReferralWidgetError
