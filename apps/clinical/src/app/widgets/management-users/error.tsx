'use client'

import { MANAGEMENT_USERS_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const ManagementUsersWidgetError = () => {
  return <WidgetError name={MANAGEMENT_USERS_WIDGET} />
}

export default ManagementUsersWidgetError
