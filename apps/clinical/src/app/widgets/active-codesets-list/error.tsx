'use client'

import { ACTIVE_CODESET_LIST_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const ActiveCodeSetListWidgetError = () => {
  return <WidgetError name={ACTIVE_CODESET_LIST_WIDGET} />
}

export default ActiveCodeSetListWidgetError
