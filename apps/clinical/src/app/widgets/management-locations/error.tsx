'use client'

import { MANAGEMENT_LOCATIONS_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const ManagementLocationsWidgetError = () => {
  return <WidgetError name={MANAGEMENT_LOCATIONS_WIDGET} />
}

export default ManagementLocationsWidgetError
