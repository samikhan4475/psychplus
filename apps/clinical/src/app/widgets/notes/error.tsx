'use client'

import { NOTES_WIDGET } from '@psychplus/widgets'
import { WidgetError } from '@psychplus/widgets/components'

const NotesWidgetError = () => {
  return <WidgetError name={NOTES_WIDGET} />
}

export default NotesWidgetError
